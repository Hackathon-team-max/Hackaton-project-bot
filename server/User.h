#pragma once

#include <cstdint>
#include <string>

struct User
{
    std::int64_t id;
    std::string lastName;
    std::string firstName;
    std::string patronymic;
    std::string address;
    std::string snils;
    std::string email;
    std::string passport;
};
